providers = ["python"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && python reindex.py && uvicorn main:app --host 0.0.0.0 --port $PORT"

# rebuild trigger
