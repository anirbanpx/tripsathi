// Sprint 3 build — auth flow (WhatsApp OTP primary, SMS + Google secondary)
// Designed in Claude Design but not wired until Sprint 3.

export default function OnboardingPage() {
  return (
    <div className="min-h-screen flex items-center justify-center px-4">
      {/* TODO: replace with Claude Design output from design-reference/OnboardingPage.html */}
      <p className="text-slate-400 text-sm">Onboarding — Sprint 3</p>
    </div>
  );
}
